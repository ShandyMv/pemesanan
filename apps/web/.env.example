# Frontend env for Vite.
# Only put deployment-specific public values here.
# Do not store secrets in frontend env because VITE_* values are exposed to the browser.

VITE_API_BASE_URL=http://localhost:3000/api
